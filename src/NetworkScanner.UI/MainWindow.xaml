<Window x:Class="NetworkScanner.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Network Scanner &amp; Monitor Manager" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status Header -->
        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10" Background="#F0F0F0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Estado del Servicio:" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock x:Name="StatusText" Text="Comprobando..." FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Foreground="Gray"/>
                <Ellipse x:Name="StatusIndicator" Width="16" Height="16" Fill="Gray" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <GroupBox Grid.Row="1" Header="Acciones del Servicio" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button x:Name="BtnInstall" Content="Instalar Servicio" Width="120" Height="30" Margin="5" Click="BtnInstall_Click"/>
                <Button x:Name="BtnUninstall" Content="Desinstalar" Width="120" Height="30" Margin="5" Click="BtnUninstall_Click"/>
                <Button x:Name="BtnStart" Content="Iniciar" Width="100" Height="30" Margin="5" Click="BtnStart_Click"/>
                <Button x:Name="BtnStop" Content="Detener" Width="100" Height="30" Margin="5" Click="BtnStop_Click"/>
            </StackPanel>
        </GroupBox>

        <!-- Logs / Config -->
        <TabControl Grid.Row="2">
            <TabItem Header="Logs (Recientes)">
                <TextBox x:Name="LogOutput" IsReadOnly="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Padding="5"/>
            </TabItem>
            <TabItem Header="Configuración">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Intervalo (Minutos):" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtInterval" Margin="0,0,0,10"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="URL API (PHP):" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtApiUrl" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Ruta Script PS1:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtScriptPath" Margin="0,0,0,10"/>

                    <Button Grid.Row="3" Grid.Column="1" Content="Guardar Configuración" Width="150" HorizontalAlignment="Right" Click="BtnSaveConfig_Click"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
